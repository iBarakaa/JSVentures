// generation of random fractions 
// done with the function math.random() that returns a random decimal 0 inclusive to 1(exclusive)

function randomFraction() {
    return Math.random(); 
}

console.log(randomFraction());